<%= form_with model: documentaryfilm, url: documentaryfilms_path do |form| %>
  <% if documentaryfilm.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(documentaryfilm.errors.count, "error") %> prohibited this documentaryfilm from being saved:</h2>

      <ul>
        <% documentaryfilm.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :synopsis, style: "display: block" %>
    <%= form.text_field :synopsis %>
  </div>

  <div>
    <%= form.label :director, style: "display: block" %>
    <%= form.text_field :director %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
